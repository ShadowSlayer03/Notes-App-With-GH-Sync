
const repoName = 'Kairno-Notes';

const metadataFileName = '.kairno.json';

const allNotesFolderId = 'all-notes';

const defaultNoteTheme = 'default';

export {
    repoName,
    metadataFileName,
    allNotesFolderId,
    defaultNoteTheme
}